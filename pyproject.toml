[project]
name = "kill-ur-vibe"
version = "0.0.1"
description = "Authorized active security-assessment CLI for AI-built web apps."
requires-python = ">=3.11"
# The deterministic core (decoders, severity, egress policy) and the SDK-free tool
# core (kuv.agent.session) are pure stdlib. These deps are only needed to RUN the
# agent spine (kuv.agent.spine) against a target.
dependencies = [
    "pyyaml>=6.0",
    "claude-agent-sdk>=0.2.128,<0.3",   # message/block types the streaming hook parses are 0.2.x
    "httpx>=0.27",
    "dnspython>=2.6",
    "tldextract>=5.1",   # registrable-domain (eTLD+1) via the Public Suffix List
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "websockets>=12"]   # websockets: the ws_app fixture + its CI probe test
pdf = ["playwright>=1.44"]   # optional: automatic HTML->PDF (else a headless Chrome is used)
probe = ["websockets>=12"]     # optional: the live websocket probe (TLS uses stdlib ssl)
browser = ["playwright>=1.48"] # optional: the headless-browser render_page probe

[project.scripts]
kuv = "kuv.wizard:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/kuv"]

[tool.pytest.ini_options]
pythonpath = ["src", "."]
testpaths = ["tests"]
