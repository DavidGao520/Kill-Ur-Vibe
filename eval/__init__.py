"""Fidelity eval harness for kuv.

Scores a produced findings list against a ground-truth set on the
finding-identity key (finding_type + normalized location). Imported at the
repo root as ``from eval.fidelity import score``.
"""
